Lỗi : 
- shortcode cuisine-showcase không hiện nội dung chỉ hiện hình ảnh 
- shortcode forest-facility-showcase cũng chỉ hiển thị hình ảnh không hiển thị nội dung 
- shortcode blog post đang bị lỗi không hiển thị ra hình ảnh 
----
Lỗi: 
1. shortcode simple-slider : Description không hiển thị, image không rõ
2. shortcode service-list ảnh hơi bé, có thể để ảnh thành background cho dễ nhìn, khi đổi sang mobile chuyển sang dạng silde 1 ảnh
3. shortcode pricing: không hiển thị dữ liệu
4. shortcode service: Thu nhỏ card phòng, khi đổi sang mobile chuyển sang dạng silde 1 ảnh